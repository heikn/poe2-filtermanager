# Frontend runtime configuration (Vite)
# Copy this file to front/.env for local development.
#
# Vite only exposes env vars prefixed with VITE_ to the browser.

# Backend API base URL
VITE_API_URL=http://localhost:3000
