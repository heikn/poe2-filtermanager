# Backend runtime configuration (Express + pg)
# Copy this file to back/.env and adjust values.

# HTTP server port
PORT=3000

# Postgres connection
# When using docker-compose, DB_HOST should be "db" (the service name).
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres

# Database name to connect to.
# If you stick with the default postgres image + default DB, "postgres" is fine.
DB_NAME=postgres
